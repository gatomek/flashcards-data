name: test
on: 
    push:
    workflow_dispatch:
jobs:
  check-bats-version:
    runs-on: ubuntu-18.04
    steps:
      - run: curl -XPOST -u "${{ secrets.PAT_USERNAME}}:${{secrets.PAT_TOKEN}}" -H "Accept: application/vnd.github.everest-preview+json" -H "Content-Type: application/json" https://api.github.com/repos/gatomek/flashcards-mongodb-data-uploader/actions/workflows/build.yaml/dispatches --data '{"ref": "main"}'